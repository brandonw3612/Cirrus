<local:NetworkProxyConfigDialogContentBase
    x:Class="Cirrus.Dialogs.NetworkProxyConfigDialogContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Cirrus.Dialogs"
    xmlns:liveModels="using:Cirrus.LiveModels"
    mc:Ignorable="d">
    <StackPanel Width="320">
        <ComboBox ItemsSource="{x:Bind ViewModel.AvailableProxyModes}"
                  SelectedItem="{x:Bind ViewModel.PreferredProxyMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  HorizontalAlignment="Stretch">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="liveModels:LocalizedText">
                    <TextBlock Text="{x:Bind Localized}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Grid Margin="0 16 0 0" ColumnSpacing="8"
              Visibility="{x:Bind ViewModel.IsCustomProxyMode, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{x:Bind ViewModel.CustomProxyHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{x:Bind ViewModel.CustomProxyPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Column="1" />
        </Grid>
    </StackPanel>
</local:NetworkProxyConfigDialogContentBase>