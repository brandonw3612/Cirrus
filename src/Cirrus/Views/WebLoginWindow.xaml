<local:WebLoginWindowBase
    x:Class="Cirrus.Views.WebLoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cirrus.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="800" MinHeight="600"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Height="32" Grid.ColumnSpan="2">
            <TextBlock VerticalAlignment="Center" Margin="12 0" FontSize="12"
                       x:Uid="Views/WebLoginWindow/TitleTextBlock" />
        </Border>
        <WebView2 Grid.Row="1" Grid.ColumnSpan="2" x:Name="WebView" />
        <TextBlock x:Uid="Views/WebLoginView/InstructionTextBlock" Grid.Row="2" Margin="12" VerticalAlignment="Center"
                   Foreground="{ThemeResource SystemBaseMediumColor}" />
        <Button Grid.Row="2" Margin="12" Grid.Column="1" x:Uid="Views/WebLoginView/VerifyButton"
                Command="{x:Bind VerifyCommand}" />
        <ProgressBar Margin="0 0 0 -1" Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Bottom"
                     IsIndeterminate="{x:Bind VerifyCommand.IsRunning, Mode=OneWay}" />
    </Grid>
</local:WebLoginWindowBase>